services:
  code-docs:
    build: .
    volumes:
      - ${HOST_CODE_DIR}:/code:ro
      - ${HOST_OUTPUT_DIR}:/output
    ports:
      - "8000:8000"
    environment:
      - CODE_DIR=/code
      - OUTPUT_DIR=/output
      - SIDEBAR_STYLE=${SIDEBAR_STYLE:-tree}
      - EXCLUDE_PROJECTS=${EXCLUDE_PROJECTS:-}
    restart: unless-stopped
